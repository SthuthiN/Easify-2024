CREATE TABLE [dbo].[AssetScoring] (
    [ID]           INT           IDENTITY (1, 1) NOT NULL,
    [AssetScoring] VARCHAR (150) NULL,
    CONSTRAINT [PK_AssetScoring] PRIMARY KEY CLUSTERED ([ID] ASC)
);

