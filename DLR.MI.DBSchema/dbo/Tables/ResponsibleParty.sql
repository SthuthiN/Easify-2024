CREATE TABLE [dbo].[ResponsibleParty] (
    [ID]               INT           IDENTITY (1, 1) NOT NULL,
    [ResponsibleParty] VARCHAR (150) NULL,
    CONSTRAINT [PK_ResponsibleParty] PRIMARY KEY CLUSTERED ([ID] ASC)
);

