CREATE TABLE [dbo].[NetworkDensity] (
    [ID]             INT           IDENTITY (1, 1) NOT NULL,
    [NetworkDensity] VARCHAR (150) NULL,
    CONSTRAINT [PK_NetworkDensity] PRIMARY KEY CLUSTERED ([ID] ASC)
);

